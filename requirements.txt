fastmcp
# slack-sdk >= 3.42 is required for the live-progress feature: chat.startStream/
# appendStream/stopStream (native streaming) and the markdown_text param (so Slack
# renders Claude's markdown instead of showing it raw). slack-bolt pulls slack-sdk
# transitively; pin both so a rebuild can't silently drop to a version without them.
slack-bolt[async]>=1.28.0
slack-sdk>=3.42.0
aiohttp
pydantic-settings
pytest>=7
# The Claude Agent SDK — the in-process alternative to spawning `claude -p` and
# parsing stream-json. Selected via CLAUDE_RUNTIME=sdk (default is the CLI path).
# Rides the same subscription creds as the CLI (verified: no ANTHROPIC_API_KEY needed).
claude-agent-sdk>=0.2.116
